# Configuración del MCP server de Mission Control.
# Copiar a .env y rellenar, o (mejor) pasar estas variables directamente
# desde la configuración de Claude Code en ~/.claude.json.
# Este fichero NO se compromete — está gitignored.

MC_API_URL=https://missioncontrol-coral.vercel.app
MC_API_KEY=pon-aqui-la-misma-key-que-en-agent/.env.local
